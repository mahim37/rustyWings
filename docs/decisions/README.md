# Decision records

Short notes on choices that were not obvious, in the style of architecture
decision records. One file per decision, numbered, never edited after the
fact except to mark them superseded.

{
  "buildCommand": "(cd libs/simulation-wasm && npx wasm-pack build --release --out-dir pkg) && (cd www && npm run build)",
  "outputDirectory": "www/",
  "installCommand": "curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y && source $HOME/.cargo/env && cd www && npm install"
}